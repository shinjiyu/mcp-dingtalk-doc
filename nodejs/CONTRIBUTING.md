# 贡献指南

感谢你考虑为本项目做贡献！

## 开发环境设置

### 1. Fork 并克隆仓库

```bash
git clone https://github.com/hykfft/mcp-dingtalk-doc.git
cd mcp-dingtalk-doc/nodejs
```

### 2. 安装依赖

```bash
npm install
```

### 3. 构建项目

```bash
npm run build
```

### 4. 运行开发模式

```bash
npm run dev
```

## 代码规范

### TypeScript 编码规范

- 使用 TypeScript 严格模式
- 为所有公共 API 提供类型定义
- 避免使用 `any` 类型
- 使用有意义的变量和函数名

### 代码风格

```typescript
// ✅ 好的例子
export async function parseDocument(nodeId: string): Promise<Document> {
  const response = await httpClient.get(`/api/docs/${nodeId}`);
  return response.data;
}

// ❌ 避免
export async function parse(id: any): Promise<any> {
  const r = await httpClient.get(`/api/docs/${id}`);
  return r.data;
}
```

## 提交规范

使用语义化提交消息：

```
<type>(<scope>): <subject>

<body>

<footer>
```

### Type 类型

- `feat`: 新功能
- `fix`: Bug 修复
- `docs`: 文档更新
- `style`: 代码格式（不影响代码运行）
- `refactor`: 重构
- `perf`: 性能优化
- `test`: 测试
- `chore`: 构建过程或辅助工具变动

### 示例

```bash
feat(parser): 添加列表元素解析支持

- 实现有序列表解析
- 实现无序列表解析
- 添加嵌套列表支持

Closes #123
```

## 开发流程

### 1. 创建分支

```bash
git checkout -b feature/your-feature-name
```

### 2. 开发和测试

```bash
# 开发模式
npm run dev

# 构建
npm run build

# 测试（如果有测试）
npm test
```

### 3. 提交更改

```bash
git add .
git commit -m "feat: 你的功能描述"
```

### 4. 推送并创建 PR

```bash
git push origin feature/your-feature-name
```

然后在 GitHub 上创建 Pull Request。

## 测试

目前项目暂无自动化测试，欢迎贡献测试用例！

建议的测试方向：

- 单元测试（各个解析函数）
- 集成测试（完整的解析流程）
- E2E 测试（通过 MCP 调用）

## 文档

如果你添加了新功能，请：

1. 更新 `README.md`
2. 更新 `CHANGELOG.md`
3. 添加代码注释
4. 如果需要，添加使用示例

## 报告 Bug

请在 GitHub Issues 中报告 Bug，包括：

- 问题描述
- 复现步骤
- 预期行为
- 实际行为
- 环境信息（Node.js 版本、操作系统等）
- 相关日志或截图

## 功能请求

欢迎提出新功能建议！请在 GitHub Issues 中描述：

- 功能描述
- 使用场景
- 预期效果
- 可能的实现方式（可选）

## 代码审查

所有的 PR 都会经过代码审查，审查会关注：

- 代码质量和可读性
- 类型安全
- 性能影响
- 文档完整性
- 向后兼容性

## 发布流程

只有维护者可以发布新版本：

1. 更新版本号（`npm version patch/minor/major`）
2. 更新 `CHANGELOG.md`
3. 运行 `npm run build`
4. 运行 `npm publish`
5. 创建 Git tag
6. 创建 GitHub Release

## 许可证

通过提交 PR，你同意你的代码以 MIT 许可证发布。

## 问题？

如有任何问题，欢迎：

- 创建 GitHub Issue
- 发送邮件给维护者
- 在 PR 中询问

感谢你的贡献！🎉

