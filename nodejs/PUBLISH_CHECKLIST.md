# 📋 发布检查清单

在发布到 npm 之前，请确认以下所有项目：

## 准备工作

- [ ] 已登录 npm 账号 (`npm whoami`)
- [ ] 已在 git 上提交所有更改
- [ ] 已更新版本号 (`npm version patch/minor/major`)

## 代码质量

- [ ] 代码已通过 TypeScript 编译 (`npm run build`)
- [ ] 所有功能已测试
- [ ] 代码格式化正确
- [ ] 无未使用的导入或变量

## 文档

- [ ] README.md 已更新
  - [ ] 安装说明正确
  - [ ] 使用示例清晰
  - [ ] API 文档完整
- [ ] CHANGELOG.md 已更新
  - [ ] 包含当前版本
  - [ ] 列出所有变更
  - [ ] 标记了日期
- [ ] LICENSE 文件存在

## package.json

- [ ] name 字段正确
- [ ] version 字段已更新
- [ ] description 清晰准确
- [ ] main 指向正确的入口文件
- [ ] types 字段存在
- [ ] files 字段包含需要发布的文件
- [ ] repository 信息正确
- [ ] keywords 合适且完整
- [ ] engines 版本要求明确

## 构建产物

- [ ] dist 目录存在
- [ ] dist/index.js 存在
- [ ] dist/index.d.ts 存在
- [ ] 所有 TypeScript 文件都有对应的 .js 和 .d.ts

## 忽略文件

- [ ] .npmignore 存在并配置正确
- [ ] 不会发布敏感文件（.env, cookie.json 等）
- [ ] 不会发布源码文件（src/）
- [ ] 不会发布测试文件

## 依赖

- [ ] dependencies 只包含运行时依赖
- [ ] devDependencies 包含开发工具
- [ ] 所有依赖版本合理
- [ ] 没有不必要的依赖

## 测试发布

```bash
# 1. 运行预检查
npm run precheck

# 2. 本地打包测试
npm pack

# 3. 检查打包内容
tar -tzf mcp-dingtalk-doc-1.0.0.tgz

# 4. 在其他目录测试安装
cd /tmp
npm install /path/to/package.tgz
```

## 正式发布

```bash
# 1. 确保在正确的目录
cd nodejs

# 2. 最后检查
npm run precheck

# 3. 构建
npm run build

# 4. 发布（首次需要 --access public）
npm publish --access public

# 5. 验证
npm view mcp-dingtalk-doc

# 6. 创建 git tag
git tag v1.0.0
git push origin v1.0.0
```

## 发布后验证

- [ ] 在 npmjs.com 上查看包页面
- [ ] 尝试全局安装 `npm install -g mcp-dingtalk-doc`
- [ ] 测试命令行工具是否工作
- [ ] README 在 npm 页面显示正确
- [ ] 版本号正确

## 发布后任务

- [ ] 在 GitHub 创建 Release
- [ ] 更新项目主 README（如果需要）
- [ ] 在社交媒体宣布（可选）
- [ ] 更新相关文档链接

## 快速命令

```bash
# 完整发布流程
npm version patch          # 更新版本
# 编辑 CHANGELOG.md
npm run precheck          # 预检查
npm run build             # 构建
npm publish               # 发布
git push && git push --tags  # 推送代码和标签
```

## 紧急回滚

如果发现问题需要撤销（72小时内）：

```bash
npm unpublish mcp-dingtalk-doc@1.0.0
```

⚠️ **注意**：撤销发布会影响已经安装该版本的用户，谨慎使用！

## 常见问题

### Q: 发布时提示权限错误？
A: 确保已登录 `npm login` 并且账号有发布权限

### Q: 版本号已存在？
A: 运行 `npm version patch` 更新版本号

### Q: 包名被占用？
A: 修改 package.json 中的 name 字段，或添加自定义后缀如 `mcp-dingtalk-doc-parser`

### Q: 某些文件没有被包含？
A: 检查 package.json 中的 files 字段和 .npmignore

---

💡 **提示**：建议把这个清单打印出来，每次发布时逐项检查！

