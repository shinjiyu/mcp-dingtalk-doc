# 钉钉文档解析 MCP 服务依赖（最小化版本）
# 不包含 playwright，适合只用手动 Cookie 的场景

# MCP框架
mcp>=1.0.0

# HTTP客户端
httpx>=0.27.0

# HTML解析
beautifulsoup4>=4.12.0

# 数据验证
pydantic>=2.0.0

# SSL处理
urllib3>=2.0.0

# 注意：如果需要自动 Cookie 管理，请使用 requirements.txt
# 并在虚拟环境中安装以避免依赖冲突

