# 钉钉文档解析 MCP 服务依赖

# MCP框架
mcp>=1.0.0

# HTTP客户端
httpx>=0.27.0

# HTML解析（使用内置html.parser，无需lxml）
beautifulsoup4>=4.12.0

# 数据验证
pydantic>=2.0.0

# SSL处理
urllib3>=2.0.0

# ==========================================
# 可选依赖：自动 Cookie 管理功能
# 如果需要自动获取 Cookie，请安装：
#   pip install playwright
#   playwright install chromium
# 
# 不安装也可以正常使用，只需手动配置 Cookie
# ==========================================

